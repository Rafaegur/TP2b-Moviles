<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ciudades"
        tools:targetApi="31">
        <activity
            android:name=".VisualizarCap"
            android:exported="false"
            android:label="@string/title_activity_visualizar_cap"
            android:theme="@style/Theme.Ciudades" />
        <activity
            android:name=".BorrarCap"
            android:exported="false"
            android:label="@string/title_activity_borrar_cap"
            android:theme="@style/Theme.Ciudades" />
        <activity
            android:name=".CargarCap"
            android:exported="true"
            android:label="@string/title_activity_cargar_cap"
            android:theme="@style/Theme.Ciudades">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>